using Microsoft.Xna.Framework;

namespace Runner {
    public class CannonBall : Projectile{
        
        public CannonBall(Vector2 pos, Vector2 vel, float zPos) : base(pos, vel, zPos) {
            
        }
    }
}